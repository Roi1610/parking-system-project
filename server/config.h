#ifndef CONFIG_H
#define CONFIG_H

// Port used by the server (matches your client)
#define SERVER_PORT 13777

// Database filename (relative to the working directory where you run the server)
#define DB_FILE "data.db"

// Log filename
#define SERVER_LOG "server.log"

#endif // CONFIG_H